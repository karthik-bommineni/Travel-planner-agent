apiVersion: v1
kind: ServiceAccount
metadata:
  name: catalog-sync
  namespace: travel-planner
  annotations:
    # terraform output -raw catalog_sync_role_arn
    eks.amazonaws.com/role-arn: REPLACE_CATALOG_SYNC_ROLE_ARN
