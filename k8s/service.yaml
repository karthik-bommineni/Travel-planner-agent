apiVersion: v1
kind: Service
metadata:
  name: travel-planner
  namespace: travel-planner
  labels:
    app: travel-planner
spec:
  type: ClusterIP
  selector:
    app: travel-planner
  ports:
    - name: http
      port: 80
      targetPort: http
